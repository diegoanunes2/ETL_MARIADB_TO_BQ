[credentials_maria_db]

user_db = nome do usuario do banco
password_db = Senha do Banco de dados
host_db = mariadb.dominio.com.br
port_db = porta utilizada
db = mariadb

[credentials_google]

destino_bq = warehouse.caminho.banco_bq
cert = caminho pra o token .json do GCP
